SQLite amalgamation used by the Windows sticky-note feature.

Source: https://www.sqlite.org/
Downloaded package: sqlite-amalgamation-3530200.zip

The Visual Studio project compiles `sqlite3.c` directly, so no external SQLite
DLL is required at runtime.
